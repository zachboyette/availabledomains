<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Search domains</title>
  <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="span8 offset2">
  <h1>Find available domains</h1>
  <form method="get" action="/" class="form-inline">
    <div class="input-append">
      <input class="input-xxlarge" type="text" name="e" id="e" value="<%= params[:e] %>" placeholder="Hey Marissa. You're a panda. Try looking for a domain."
      ><button type="submit" class="btn btn-primary">Check</button>
    </div>
  </form>
  <% if params[:e] %>
    <% if @results.any? %>
      <h2>Available</h2>
      <ul>
        <% @results.each do |result| %>
          <li><%= Rack::Utils.escape result %></li>
        <% end %>
      </ul>
    <% else %>
      <p>No available domains found :(</p>
    <% end %>
  <% end %>
</div>
</body>
</html>
